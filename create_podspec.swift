import Foundation

let fileManager = FileManager()
let path = fileManager.currentDirectoryPath
print(path)
let file FileHandle? = FileHandle(forAtWritingPath: “data_support.podspec”);
FileHandle(forAtWritingPath: “data_support.txt”);
